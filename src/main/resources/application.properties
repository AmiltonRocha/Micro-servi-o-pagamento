spring.application.name=PAGAMENTO
server.port=${PORT:8080}

# Configuração do Eureka
eureka.instance.appname=PAGAMENTO
eureka.instance.hostname=${RAILWAY_PRIVATE_IP:localhost}
eureka.instance.ip-address=${RAILWAY_PRIVATE_IP:127.0.0.1}
eureka.instance.virtual-host-name=pagamento-service
eureka.instance.prefer-ip-address=true
eureka.instance.status-page-url=${RAILWAY_PUBLIC_DOMAIN:http://localhost:${server.port}}/info
eureka.instance.health-check-url=${RAILWAY_PUBLIC_DOMAIN:http://localhost:${server.port}}/health

# Configuração do Cliente Eureka
eureka.client.service-url.defaultZone=https://eurekaronaldo-production-b958.up.railway.app/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.client.ssl.enabled=true

# Configuração do Data Center
eureka.datacenter=MyOwn

# Configurações do Banco de Dados
spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway
spring.datasource.username=root
spring.datasource.password=dFdirtwzLXuHbByhJxANFRywBdKYseRR
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.unifor.pagamento=DEBUG

